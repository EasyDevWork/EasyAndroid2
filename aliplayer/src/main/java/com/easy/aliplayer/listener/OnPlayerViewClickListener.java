package com.easy.aliplayer.listener;

import com.easy.aliplayer.base.ScreenMode;
import com.easy.aliplayer.base.PlayViewType;

public interface OnPlayerViewClickListener {
    void onClick(ScreenMode screenMode, PlayViewType viewType);
}
